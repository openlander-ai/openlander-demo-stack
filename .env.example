# OpenLander detects the pg and redis package dependencies and can provision both.
# Leave these empty in git; OpenLander injects real connection strings at deploy time.
DATABASE_URL=
REDIS_URL=
PORT=3000
